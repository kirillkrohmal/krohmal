В продолжение нашего телефонного общения направляю наши тестовые задачи во вложении и  сопроводительный текст ниже.

1. Необходимо провести рефакторинг следующей структуры данных, создав структуру классов с использованием принципов
ООП: Map<Map<String,Map>, List<Map<String, String>>> benfAndCatToCtx = new HashMap();

2. Необходимо переписать класс во вложении (Example1_Test.java) применив интерфейсы, наследование и конструктор.

3. Метод createUser класса CASoap (во вложении Example2_CASoap) возвращает объект ResponseCASoap
(во вложении Example2_ResponseCASoap).

Объект ResponseCASoap содержит в себе лог (String log).

По факту выполнения, метод createUser вернул объект ResponseCASoap с пустым логом, при этом точно известно,
что на этапе выполнения метода createUser лог не пустой и попадает в возвращаемый объект ResponseCASoap

Необходимо найти и указать на ошибку.

4. У сущности Роль есть атрибуты  «Компания», «Система», «Тип роли», каждый из которых может принимать только
одно значение из большого справочника. Необходимо сформулировать функциональные требования к следующим пользовательским
требованиям: «Нужен интерфейс поиска ролей с фильтрами-выпадающими списками: Система, Компания, Тип роли. При выборе значения
в одном из фильтров, в других фильтрах должны динамически сужаться перечни возможных значений».



Если удастся решить 1-2 задачи, это уже для нас хороший показатель.



Как обсудили, буду ждать подтверждение письма и сколько времени потребуется на выполнение теста.