Добавить метод boolean isBinary().
Метод должен проверять количество дочерних элементов в дереве. Если их <= 2 - то дерево бинарное.
метод должен циклически пройти по всем элементам дерева.

Для начала сделай правильно дерево. Один корневой узел, у него список дочерних узлов.
У каждого дочернего узла, могут быть свои дочерние узлы (хранимые в списке).

Нужно написать метод isBinary, который будет проходить по всем узам дерева и проверять, что число дочерних узлов
не больше двух. Если это так то возвращает true, если хотя бы у одного узла число дочерних
узлов превышает 2, то возвратить false.