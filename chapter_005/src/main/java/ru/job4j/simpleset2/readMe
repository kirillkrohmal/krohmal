Реализовать коллекцию SimpleSet.
Коллекция должна обеспечивать void add(E e) и реализовывать Iterator<E>.
Коллекция не должна хранить дубликаты.
Set - внутри для хранения данных использует связный список.

в этом задании нельзя использовать готовые коллекции jdk.
Ты же до этого реализовывал свой вариант связанного списка - вот его можешь использовать.


1) Почему ты дубликат ищешь в simpleSet, а добавляешь в list ? Какой в этом смысл?

2) Почему ты используешь не инициализированный simpleSet ?

Кирилл, методы add, isDuplicate реализованы верно, за исключением того, что у тебя isDuplicate ищет в цикле пока i < size;
а size  у тебя никак не модифицируется при добавлении элементов. Соответственно в коллекцию будут попадать дубликаты.
Поэтому при добавлении элемента тебе надо инкрементировать size. Что касается hasNext: у тебя там такая запись Node<E> eNode = el;
- обрати внимание, что el никак не инициализировано, поэтому у тебя hasNext всегда будет возвращать false, хотя элементы в коллекции
присутствуют. И соответственно при вызове метода next, hasNext возвращает false и next кидает NoSuchElementException.
Но даже если бы у тебя hasNext возвращал true, т.к. элемент el не инициализирован, у тебя было бы NullPointerException
вот в этой строке element = el.value;


